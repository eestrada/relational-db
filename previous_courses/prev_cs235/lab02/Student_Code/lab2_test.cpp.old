#include <cstdlib>
#include <string>
#include <iostream>

#include "JosephusInterface.h"
#include "Circle.h"

using namespace::std;

void printVector(vector<string>& strvec)
{
    int vecsize = strvec.size();

    for(int i = 0; i < vecsize; i++)
    {
        cout << strvec[i] << endl;
    }
}


int main(int argc, const char* argv[])
{

    Circle* test = new Circle();

    vector<string> testNames = test->getNames();

    printVector(testNames);

    return 0;

}
