This program acts like a unix filter. Thus it should be used in the following way (the $ sign represents the terminal prompt):

    $ cat input.txt | ethans_final > OUTPUT.txt

I found it easier to program as a filter than to try to ask the user for input.
